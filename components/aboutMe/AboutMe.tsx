import AboutSection from './AboutSection';
import HeroSection from './HeroSection';

const AboutMe = () => {
  return (
    <section>
      <HeroSection />
      <AboutSection />
    </section>
  );
};

export default AboutMe;
